import React from 'react'
import LoginForm from '../shared/login/login.redux'
import styles from './nav.css'

const nav = ({ onChange }) => (
	<div className={ styles.nav }>
		<h1>Threads Authentication System</h1>
	</div>
)

export default nav
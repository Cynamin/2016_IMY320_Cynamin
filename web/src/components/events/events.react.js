import React from 'react'
import styles from './events.css'

const events = () => (
	<div className={ styles.container }>
		<div className={ styles.content }>
			<h1 className={ styles.pageHeader }>Events</h1>
		</div>
	</div>
)

export default events